---
label: Order
fields:
- name: order
  type: blocks
  label: Order
  template_types:
  - project
  config:
    min: 
    max: 
- name: sortable_list
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: projects
  label: Sortable List
pages:
- _data/project-order.yaml
