---
label: Order
hide_body: true
fields:
- name: sortable_list
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: projects
  label: Project Order
  description: The projects you choose here will show up first in the assigned order.
    Everything else will show up after this list in alphabetical order.
pages:
- _data/project-order.yaml
